<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - Buyer Portal</title>

  <style>
    * {
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }

    body {
      margin: 0;
      height: 100vh;
      display: flex;
      align-items: center;     /* Perfect center vertically */
      justify-content: center; /* Perfect center horizontally */
      background: linear-gradient(135deg, #e0ffe0, #c8e6c9);
    }
    .center-wrapper {
  width: 100%;
  display: flex;
  align-items: center;       /* vertical center */
  justify-content: center;   /* horizontal center */
}


    .container {
      background: #ffffff;
      padding: 40px;
      border-radius: 15px;
      width: 320px;
      text-align: center;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      animation: slideIn 1s ease-out;
    }

    @keyframes slideIn {
      from { transform: translateY(30px); opacity: 0; }
      to   { transform: translateY(0); opacity: 1; }
    }

    h2 {
      margin-bottom: 18px;
      color: #2e7d32;
      font-size: 22px;
      font-weight: 600;
    }

    input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #d6dbe8;
      border-radius: 8px;
      background-color: #f7f7f7;
      font-size: 15px;
      transition: 0.3s ease;
    }

    input:focus {
      border-color: #43a047;
      box-shadow: 0 6px 18px rgba(76, 175, 80, 0.15);
      background-color: #fff;
      outline: none;
    }

    button {
      width: 100%;
      padding: 12px;
      background: linear-gradient(180deg, #43a047, #388e3c);
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      margin-top: 5px;
      transition: 0.3s ease;
      box-shadow: 0 8px 18px rgba(56,142,60,0.12);
    }

    button:hover {
      background: #2e7d32;
      box-shadow: 0 12px 28px rgba(56,142,60,0.18);
    }

    .small-text {
      margin-top: 12px;
      font-size: 14px;
      color: #555;
    }

    a {
      color: #2e7d32;
      font-weight: 600;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <div class="center-wrapper">
    <div class="container">
      <h2>Buyer Login</h2>

      <form method="post" action="/users/loginbuyer">
        <input type="text" name="username" placeholder="Username" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Login</button>
      </form>

      <div class="small-text">
        Don't have an account? <a href="/users/signupbuyer">Sign Up</a>
      </div>
    </div>
  </div>
</body>

</html>
